body{background-color:#f3f3f3; margin:0;}
.head{
  width:100%;
  height:100px;
  background:url(../images/members/members.jpg);
}
.tag{
  clear:both;
  position:relative;
  background-color:#fff;
  padding: 3%;
}
.tag em{
  display:block; 
  position:absolute;
  width: 0;
  height: 0;
  border-left: 15px solid transparent;
  border-right: 15px solid transparent;
  border-bottom: 40px solid #FFF;
  font-size:0;
  line-height:0;
}
.tag left{}
.tag right{}
.tag .clear{clear:both;}
.headimg{
  float:left;
  line-height:0px;
  margin-left:5%;
  margin-right:-5%;
}
                           
.left{
  text-align:center;
  float:left;
  width:5%;
  margin-right:-5%;
}

/* entire container, keeps perspective */
.flip-container {
	perspective: 1000;
    -webkit-perspective: 1000;
    width:100%;
    line-height:0px;
}
	/* flip the pane when hovered */
.flip-container:hover .flipper, .flip-container.hover .flipper {
	transform: translateX( -100% ) rotateY(180deg);
}


/* flip speed goes here */
.flipper {
	transition: 0.3s;
	transform-style: preserve-3d;
    transform-origin: right center;
	position: relative;
}

/* hide back of pane during swap */
.front, .back {
  backface-visibility: hidden;
}
.back{
  position: absolute;
  top: 0;
  left: 0;
  width:100%;
  height:85%;
  background:#8173bc;
  color:white;
  padding-top:15%;
  text-align:center;
}

/* back, initially hidden pane */
.back {
	transform: rotateY(180deg);
}

@media screen and (min-width: 0px) {
  .headimg{width:30%;}
  .tag{width:80%;padding:10%;}
  .tag left{
    text-align:center;
    padding:10%;
  }
  .tag em{
    top:-15px;
    border-left: 6px solid transparent;
    border-right: 6px solid transparent;
    border-bottom: 15px solid #FFF;
  }
}
@media screen and (min-width: 768px) { 
  .headimg{width:18%;}
  .tag{width:94%; padding:3%;}
  .tag left{
    float:left;
    width:30%;
    margin-right:10%;
    padding:0px;
  }
  .tag right{
    float:left;
    width:60%;
  }
  .tag em{
    top:-30px;
    border-left: 12px solid transparent;
    border-right: 12px solid transparent;
    border-bottom: 30px solid #FFF;
  }
}
.card{
  width:100%;
  border:1px solid #8173bc;
  border-radius:3px;
  background:#8173bc;
}
.card .top{color:white; text-align:center;}
.card .img{line-height:0px;}
.card .tail{background:#f3f3f3; text-align:center; padding:10px; border-bottom-right-radius:3px; border-bottom-left-radius:3px;}
.card .button{
  border:none;
  -moz-box-shadow: 0px 1px 0px 0px #ccc;
  -webkit-box-shadow: 0px 1px 0px 0px #ccc;
  box-shadow: 0px 1px 0px 0px #ccc;
  text-align:center;
  width:60px;
  background-color:#8173bc;
  -moz-border-radius:18px;
  -webkit-border-radius:18px;
  border-radius:18px;
  cursor:pointer;
  color:#ffffff;
  font-family:Arial;
  font-size:17px;
  padding:5px 10px;
  text-decoration:none;
}
.card .button:active {
	background-color:purple; 
}

.discription{
  width:100%;
}


